return {
   config = require("stealth.config"),
   layers = require("stealth.collision_layers"),
   World = require("stealth.world"),
   Movement = require("stealth.movement"),
   Perception = require("stealth.perception"),
   Visibility = require("stealth.visibility"),
   Triggers = require("stealth.triggers"),
   DebugDraw = require("stealth.debug_draw"),
}
